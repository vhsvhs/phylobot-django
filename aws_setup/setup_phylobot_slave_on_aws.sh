#
# This script should be run when the PhyloBot slave node 
# instance is launched for the first time.
#

cd ~/
sudo rm -rf repository/asr-pipeline
cd repository
git clone https://github.com/vhsvhs/asr-pipeline
cd ~/
